import React from 'react';

// PUBLIC_INTERFACE
export default function Security() {
  return (
    <div>
      <h1 className="h2">Security</h1>
      <p className="text-muted">Security posture, IAM risks, and policy enforcement.</p>
      <div className="surface" style={{ padding: 'var(--space-6)', marginTop: 'var(--space-4)' }}>
        <p className="text-muted">Security dashboard placeholder.</p>
      </div>
    </div>
  );
}
