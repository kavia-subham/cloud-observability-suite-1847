import React from 'react';

// PUBLIC_INTERFACE
export default function Settings() {
  return (
    <div>
      <h1 className="h2">Settings</h1>
      <p className="text-muted">Workspace configuration and preferences.</p>
      <div className="surface" style={{ padding: 'var(--space-6)', marginTop: 'var(--space-4)' }}>
        <p className="text-muted">Settings form placeholder.</p>
      </div>
    </div>
  );
}
