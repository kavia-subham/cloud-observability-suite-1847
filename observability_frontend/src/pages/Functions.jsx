import React from 'react';

// PUBLIC_INTERFACE
export default function Functions() {
  return (
    <div>
      <h1 className="h2">Functions</h1>
      <p className="text-muted">Browse and inspect serverless functions across providers.</p>
      <div className="surface" style={{ padding: 'var(--space-6)', marginTop: 'var(--space-4)' }}>
        <p className="text-muted">Table/list placeholder.</p>
      </div>
    </div>
  );
}
